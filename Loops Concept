- hosts:
  tasks:
   - name: install softwares
      apt:
       name: "{{item}}"
       state: present
 with_items:
  - wget
  - zip
  - unzip

- hosts: 
  tasks:
- name: install wget, zip and unzip
 apt:
  - name: "{{item}}"
    state: present 
with_items:
- wget
- zip 
- unzip


  
